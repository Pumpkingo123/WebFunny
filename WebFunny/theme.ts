import type { GlobalThemeOverrides } from 'naive-ui'
const themeOverrides: GlobalThemeOverrides = {
  common: {
    primaryColor: '#ff7626',
    primaryColorHover: '#fff1e9',
    primaryColorPressed: '#ff7626'
    // primaryColorSuppl: '#4098FC'
  },
  Button: {
      textColor: '#FF0000'
  }
}
 
export default themeOverrides